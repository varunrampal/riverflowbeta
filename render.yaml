services:
  - type: web
    name: riverflow
    runtime: static
    buildCommand: npm ci && npm run build
    staticPublishPath: ./dist
    domains:
      - riverflowlaser.com
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
